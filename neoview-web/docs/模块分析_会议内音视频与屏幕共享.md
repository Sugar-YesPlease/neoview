# 会议内音视频与屏幕共享 模块分析

## 1. 功能概述 (Functional Overview)
该模块负责会议中的媒体控制与展示：本地麦克风/摄像头开关、远端音视频消费展示、屏幕共享开始与停止、共享异常检测恢复、参会者媒体状态联动。

## 2. 页面跳转流程 (Page Transition Flow)
```mermaid
stateDiagram-v2
    state "会议主界面" as Main
    state "媒体已发布" as Published
    state "屏幕共享中" as Sharing
    state "共享异常恢复" as Recovering

    Main --> Published : "开启麦克风或摄像头"
    Published --> Sharing : "开启屏幕共享"
    Sharing --> Published : "停止屏幕共享"
    Sharing --> Recovering : "检测到共享卡顿"
    Recovering --> Published : "清理共享状态"
```

## 3. 接口清单 (API List)
| Interface Description | URI | Method | Parameter Description | Code Reference |
| :--- | :--- | :--- | :--- | :--- |
| 发布音频/视频轨道 | `protooRequest:produce` | WS Request | `transportId, kind, rtpParameters, appData` | `src/services/mediasoupSession.js` |
| 广播房间媒体状态 | `type: roomMedia` | WS Send | `kind, enabled` | `src/services/mediasoupSession.js` |
| 广播共享结束 | `type: screenShareStopped` | WS Send | `reason` | `src/services/mediasoupSession.js` |
| 接收新消费者 | `protooServerRequest:newConsumer` | WS Receive | `peerId, producerId, kind, rtpParameters, appData` | `src/services/mediasoupSession.js` |

## 4. 业务逻辑时序图 (All Business Logic)
### 4.1 开关麦克风
```mermaid
sequenceDiagram
    participant U as "用户"
    participant UI as "MeetingRoom.vue"
    participant A as "App.vue"
    participant MS as "mediasoupSession.js"

    U->>UI: "点击麦克风按钮"
    UI->>A: "emit(toggle-mic)"
    A->>MS: "enableMic或disableMic"
    MS-->>A: "返回本地流或关闭完成"
    A-->>UI: "更新micEnabled与本地媒体状态"
```

### 4.2 开关摄像头
```mermaid
sequenceDiagram
    participant U as "用户"
    participant UI as "MeetingRoom.vue"
    participant A as "App.vue"
    participant MS as "mediasoupSession.js"

    U->>UI: "点击摄像头按钮"
    UI->>A: "emit(toggle-cam)"
    A->>MS: "enableCam或disableCam"
    MS-->>A: "返回本地流或关闭完成"
    A-->>UI: "更新camEnabled与视频画面"
```

### 4.3 屏幕共享开始
```mermaid
sequenceDiagram
    participant U as "用户"
    participant UI as "MeetingRoom.vue"
    participant A as "App.vue"
    participant MS as "mediasoupSession.js"

    U->>UI: "点击共享屏幕"
    UI->>A: "emit(toggle-screen-share)"
    A->>MS: "enableScreenShare"
    MS-->>A: "返回screenShareStream"
    A-->>UI: "更新screenShareActive并切换布局"
```

### 4.4 屏幕共享停止
```mermaid
sequenceDiagram
    participant U as "用户"
    participant A as "App.vue"
    participant MS as "mediasoupSession.js"
    participant UI as "MeetingRoom.vue"

    U->>A: "触发停止共享"
    A->>MS: "disableScreenShare"
    A->>MS: "notifyScreenShareStopped(reason)"
    A->>A: "clearScreenShareState"
    A-->>UI: "恢复普通布局"
```

### 4.5 远端流消费并渲染
```mermaid
sequenceDiagram
    participant SFU as "服务端"
    participant MS as "mediasoupSession.js"
    participant A as "App.vue"
    participant UI as "MeetingRoom.vue"

    SFU->>MS: "下发newConsumer请求"
    MS-->>A: "onState(type=consumer, stream)"
    A->>A: "更新remoteVideos或remoteAudios"
    A-->>UI: "通过props下发远端流"
    UI->>UI: "watch后绑定video或audio元素"
```

## 5. 数据模型 (ER Diagram)
```mermaid
erDiagram
    LOCAL_MEDIA {
        boolean micEnabled
        boolean camEnabled
        boolean aiDenoiseEnabled
        boolean virtualAvatarEnabled
    }
    REMOTE_MEDIA {
        string peerId
        string displayName
        boolean videoPaused
        boolean audioPaused
    }
    SCREEN_SHARE {
        boolean active
        string ownerPeerId
        string ownerDisplayName
    }

    LOCAL_MEDIA ||--o{ REMOTE_MEDIA : "发布后被消费"
    REMOTE_MEDIA ||--o| SCREEN_SHARE : "可扩展为共享源"
```
