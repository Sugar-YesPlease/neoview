# 认证与会议入口 模块分析

## 1. 功能概述 (Functional Overview)
该模块负责用户身份建立与会议入口控制，覆盖登录、注册、创建会议、输入会议号加入会议、退出登录，并驱动页面在“认证页/大厅页/会议页”之间切换。

## 2. 页面跳转流程 (Page Transition Flow)
```mermaid
stateDiagram-v2
    state "认证页" as Auth
    state "会议大厅" as Hall
    state "会议中" as InCall

    Auth --> Hall : "登录或注册成功"
    Hall --> InCall : "加入会议成功"
    InCall --> Hall : "离开会议"
    Hall --> Auth : "退出登录"
```

## 3. 接口清单 (API List)
| Interface Description | URI | Method | Parameter Description | Code Reference |
| :--- | :--- | :--- | :--- | :--- |
| 用户注册 | `/api/auth/register` | POST | `username, password, displayName` | `src/services/api.js` |
| 用户登录 | `/api/auth/login` | POST | `username, password` | `src/services/api.js` |
| 创建会议 | `/api/meetings` | POST | `title` | `src/services/api.js` |

## 4. 业务逻辑时序图 (All Business Logic)
### 4.1 登录
```mermaid
sequenceDiagram
    participant U as "用户"
    participant A as "App.vue"
    participant API as "api.js"

    U->>A: "提交登录表单"
    A->>API: "login(payload)"
    API-->>A: "返回token与displayName"
    A->>A: "setAuth并写入localStorage"
    A-->>U: "进入会议大厅"
```

### 4.2 注册
```mermaid
sequenceDiagram
    participant U as "用户"
    participant A as "App.vue"
    participant API as "api.js"

    U->>A: "提交注册表单"
    A->>API: "register(payload)"
    API-->>A: "返回token与displayName"
    A->>A: "setAuth并写入localStorage"
    A-->>U: "进入会议大厅"
```

### 4.3 创建会议
```mermaid
sequenceDiagram
    participant U as "用户"
    participant A as "App.vue"
    participant API as "api.js"

    U->>A: "点击创建会议"
    A->>API: "createMeeting({title})"
    API-->>A: "返回roomId"
    A-->>U: "回填会议号并可直接加入"
```

### 4.4 加入会议
```mermaid
sequenceDiagram
    participant U as "用户"
    participant A as "App.vue"
    participant MS as "mediasoupSession.js"

    U->>A: "输入会议号并点击加入"
    A->>MS: "new MediasoupSession + join(roomId)"
    MS-->>A: "返回连接成功状态"
    A-->>U: "切换到会议界面"
```

### 4.5 退出登录
```mermaid
sequenceDiagram
    participant U as "用户"
    participant A as "App.vue"
    participant MS as "mediasoupSession.js"
    participant ASR as "asrStreamer.js"

    U->>A: "点击退出登录"
    A->>ASR: "stop()"
    A->>MS: "close()"
    A->>A: "清理本地状态与localStorage"
    A-->>U: "返回认证页"
```

## 5. 数据模型 (ER Diagram)
```mermaid
erDiagram
    AUTH_STATE {
        string token
        string displayName
        string username
    }
    LOBBY_STATE {
        string joinRoomId
        string meetingTitle
        boolean joining
    }
    CALL_STATE {
        boolean callActive
        string roomId
        string peerId
    }

    AUTH_STATE ||--|| LOBBY_STATE : "登录后进入"
    LOBBY_STATE ||--|| CALL_STATE : "加入会议后进入"
```
